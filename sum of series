#include <stdio.h>

int main() {
    int n, i;
    double sum = 0.0;
    printf("Enter the number of terms (n): ");
    scanf("%d", &n);
    for (i = 1; i <= n; i++) {
        sum += 1.0 / i; 
    }
    printf("The sum of the series up to %d terms is: %lf\n", n, sum);

    return 0;
}
